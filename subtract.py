""" This function accept inputs as integers and subtracts"""


def subtra(a, b):
    """This function accept inputs as integers and subtracts"""
    a = int(a)
    b = int(b)
    c = a + b
    print("Subtraction of {a} and {b} is {c}")
    return c
